name: workflow1

on:

  push:
    #branches: [ "main" ]

jobs:

  construir:

    runs-on: ubuntu-latest

    steps:

      - name: Run a one-line script
        run: echo Hello, world!

      - name: Imprimir multiples líneas
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
